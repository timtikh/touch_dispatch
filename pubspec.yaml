name: touch_dispatch
description: TouchDispatch Flame game
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.22.0

flutter:
  assets:
    - assets/images/plane_colored.png
    - assets/images/runway.png
